"use client";
import css from "./ErrorMessage.module.css";

const ErrorMessage = () => {
  return (
    <div className={css["errorMessage"]}>
      <ErrorMessage />
    </div>
  );
};

export default ErrorMessage;
